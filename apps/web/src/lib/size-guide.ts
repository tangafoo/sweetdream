/**
 * PLACEHOLDER MEASUREMENTS — fill these in when the real dimensions are
 * confirmed (strings render as-is, e.g. "153 cm"). Any null shows as "—"
 * and keeps the "measurements coming soon" note visible on product pages.
 */
export const SIZE_DIMENSIONS: Record<string, { width: string | null; length: string | null }> = {
  Single: { width: null, length: null },
  Twin: { width: null, length: null },
  Queen: { width: null, length: null },
  King: { width: null, length: null },
};

export const DIM_PLACEHOLDER = "—";
